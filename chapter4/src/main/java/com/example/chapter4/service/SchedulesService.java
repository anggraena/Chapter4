package com.example.chapter4.service;

import com.example.chapter4.model.Schedules;
import com.example.chapter4.model.Users;

import java.util.Iterator;
import java.util.List;

public class SchedulesService {
    public List<Users> getSchedulesByActive() {
        return getSchedulesByActive();
    }

    public int saveSchedules(Schedules schedules) {
        return saveSchedules(schedules);
    }

    public Users addSchedule(Schedules schedules) {
        return addSchedule(schedules);
    }

    public List<Users> addSchedules(List<Schedules> schedules) {
        return addSchedules(schedules);
    }

    public Iterator<Schedules> getAllSchedules() {
        return getAllSchedules();
    }

    public String deleteId(int id) {
        return deleteId(id);
    }

    public String updateSchedules(Schedules schedules) {
        return updateSchedules(schedules);
    }
}
