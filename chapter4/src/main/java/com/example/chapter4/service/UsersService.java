package com.example.chapter4.service;

import com.example.chapter4.model.Users;

import java.util.Iterator;
import java.util.List;

public class UsersService {
    public List<Users> getUsersByActive() {
        return getUsersByActive();
    }

    public int saveUsers(Users users) {
        return saveUsers(users);
    }


    public Users addUser(Users users) {
        return addUser(users);
    }

    public List<Users> addUsers(List<Users> users) {
        return addUsers(users);
    }

    public Iterator<Users> getAll() {
        return getAll();
    }

    public String updateUser(Users users) {
        return updateUser(users);
    }

    public List<Users> getNamaUser(Users users) {
        return getNamaUser(users);
    }

    public String deleteId(int id) {
        return deleteId(id);
    }
}
