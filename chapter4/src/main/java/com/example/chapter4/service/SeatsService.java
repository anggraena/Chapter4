package com.example.chapter4.service;

import com.example.chapter4.model.Seats;
import com.example.chapter4.model.Users;

import java.util.Iterator;
import java.util.List;

public class SeatsService {
    public List<Users> getSeatsByActive() {
        return getSeatsByActive();
    }

    public int saveSeats(Seats seats) {
        return saveSeats(seats);
    }

    public Users addSeat(Seats seats) {
        return addSeat(seats);
    }

    public List<Seats> addSeats(List<Seats> seats) {
        return addSeats(seats);
    }

    public Iterator<Seats> getAllSeats() {
        return getAllSeats();
    }

    public List<Users> getNamaStudio(Seats seats) {
        return getNamaStudio(seats);
    }

    public String deleteId(int id) {
        return deleteId(id);
    }

    public String updateSeats(Seats seats) {
        return updateSeats(seats);
    }
}
